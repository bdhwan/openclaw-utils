# openclaw-utils

Utility scripts for OpenClaw agents.

## Tools

### [firebase-storage-uploader](./firebase-storage-uploader/)

Upload files to Firebase Storage and get public download URLs.

```bash
cd firebase-storage-uploader
pip install -r requirements.txt
python firebase_upload.py -e .env -s file.zip -d uploads/file.zip
```

## License

MIT
